[runtime]
language    = "python"          # e.g. python, typescript, go
base_image  = "python:3.14-slim"          # e.g. python:3.12-slim, node:20-slim
install_cmd = "pip install -e '.[dev]'"
test_cmd    = "pytest"
build_cmd   = ""
lint_cmd    = "ruff check ."

[bureau]
constitution = ".specify/memory/constitution.md"
